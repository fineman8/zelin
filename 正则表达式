电话号码：
邮箱：[a-zA-Z]+://\S*
#*零个或多个
#+一个或多个
.任意字符，除了换行符
[1,2,3,4,5,6,7]在[]里取
^H开头是H
$结尾
|或者

python正则库re(自带的)
1.match
import re 
content='qweqweq'
result=re.match(r'正则表达式',cotent)
print(result.group())
print(result.span())
2.匹配目标
r=re.match(r'Hello\s(\d+)\s',content)
r.group

2.通用匹配
result=re.match(r'H.*(\d{7})\s',content)
.*匹配所有 贪婪模式
.*? 非贪婪
